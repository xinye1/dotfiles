[Unit]
Description=herdr: copy session.json when it changed (skips an emptied session)
ConditionPathExists=%h/.config/herdr/session.json

[Service]
Type=oneshot
ExecStart=%h/.local/bin/herdr-session-backup
